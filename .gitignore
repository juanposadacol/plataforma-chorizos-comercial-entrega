node_modules/
dist/
.env
.env.local
.env.*.local
coverage/
.netlify/
.supabase/
# Metadata local del Supabase CLI; no describe el proyecto remoto.
supabase/.branches/
supabase/.temp/
*.log
!.env.example


.env.local

# Parches de entrega (cambios-N.patch). Son artefactos para aplicar desde
# otra maquina, no contenido del repositorio: versionarlos mete dentro de un
# commit una copia de ese mismo commit.
*.patch
